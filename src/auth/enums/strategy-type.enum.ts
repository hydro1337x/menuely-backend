export enum StrategyType {
  USER_LOCAL = 'user-local',
  USER_ACCESS_JWT = 'user-access-jwt',
  REFRESH_JWT = 'refresh-jwt',
  RESTAURANT_LOCAL = 'restaurant-local',
  RESTAURANT_ACCESS_JWT = 'restaurant-access-jwt'
}
